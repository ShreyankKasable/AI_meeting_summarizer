# Dev: leave VITE_API_BASE_URL empty and point VITE_API_PROXY at the backend
# (Vite proxies /api and /socket.io to it). Prod: set VITE_API_BASE_URL instead.
VITE_API_PROXY=http://localhost:5000
VITE_API_BASE_URL=
